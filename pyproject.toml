[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "transport_analysis"
version = "0.1.0"
description = "Transport analysis tools"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["transport_analysis*"]